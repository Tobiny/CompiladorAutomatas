# Simple Language Compiler Dependencies
# Python 3.7+ required

# Standard library modules used:
# - re (regex operations)
# - os (file operations)
# - sys (system operations)
# - pathlib (path handling)
# - typing (type hints)
# - dataclasses (data structures)
# - enum (enumerations)

# Optional dependencies for development and testing:
pytest>=6.0.0
pytest-cov>=2.12.0

# Code quality tools
black>=21.0.0
flake8>=3.9.0
mypy>=0.910

# Documentation (optional)
sphinx>=4.0.0
sphinx-rtd-theme>=0.5.0 